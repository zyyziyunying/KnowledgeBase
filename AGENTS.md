# 知识库维护约定

## 阅读顺序

1. 先读根目录 [README.md](README.md)，确认目标内容所属领域。
2. 再读最近一级的领域 `README.md` 和专题 `README.md`。
3. 最后阅读并编辑具体内容文档。

上层索引只保存稳定边界、阅读顺序和链接；可变结论、当前内容与验证证据只写在最接近主题的事实源中。

## 目录边界

- `knowledge/<domain>/README.md`：领域范围和专题入口。
- `knowledge/<domain>/<topic>/README.md`：专题范围、当前事实源、阅读顺序和拆分状态。
- 专题内容文档：具体知识、证据、源码入口、适用版本和边界。
- 不创建没有实际内容的目录或占位文档。

Android Framework 只是 Android 领域下的一个专题。新增其他领域或 Android 专题时，应建立同级目录，不要继续扩张现有 Framework 讲义。

## 内容维护

- 优先修订既有事实源，不创建表达同一主题的平行文档。
- 新结论尽量包含机制、可复查入口和适用边界；不确定内容明确标注待验证。
- 本地绝对路径、密钥、账号和机器状态不写进知识正文。
- 内部链接使用相对路径；移动或拆分文档时同步修复入口和反向链接。
- 保留既有文件名，除非重命名能明显改善长期导航，并且同次完成链接迁移。

## 拆分规则

不要仅按行数机械拆分。满足以下任一条件时，可以把专题文档拆成多篇：

- 不同章节已经需要独立阅读、验证或更新；
- 单文件明显妨碍检索、审阅或定位事实源；
- 多个新文档反复链接到其中不同章节；
- 章节拥有不同的版本边界或维护节奏。

拆分时，由专题 `README.md` 承担阅读顺序；内容应迁移而不是复制。若原路径已被其他内容引用，保留一个简短迁移页或一次性修复全部已知链接。

## 完成前检查

- 运行 `git diff --check`。
- 确认新增或修改的 Markdown 相对链接均可解析。
- 确认索引没有复制具体文档中的可变结论。
- 若只做静态文档检查，明确说明尚未验证的设备、源码分支或运行时事实。
